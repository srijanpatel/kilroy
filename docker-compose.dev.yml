services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: kilroy
      POSTGRES_PASSWORD: kilroy
      POSTGRES_DB: kilroy
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kilroy"]
      interval: 2s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
